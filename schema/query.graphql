type Query {
  currentUser(id: String): User
  getCandles(
    symbol: String!
    from: String!
    to: String!
    resolution: String!
  ): [Candle!]!
  getAllIndexData(
    from: String!
    to: String!
    resolution: String!
  ): [CandleSet!]!
}
