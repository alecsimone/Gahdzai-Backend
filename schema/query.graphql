type Query {
  currentUser(id: String): User
  getCandles(
    symbol: String!
    from: String!
    to: String!
    resolution: String!
  ): [Candle]
  getAllIndexCandles(
    from: String!
    to: String!
    resolution: String!
  ): [CandleCollection]
}
